#!/usr/bin/env script.js

// see docs/Development.md

// Test
app.cmd('tw', 'Run tests in watch mode').add(({ argv }) => {
  $`bun run test:watch ${argv}`
})

app.cmd('t', 'Run tests once').add(({ argv }) => {
  $`bun run test ${argv}`
})

app.cmd('l', 'Run lint').add(({ argv }) => {
  $`bun run lint ${argv}`
})
