#!/usr/bin/env script.js

import '/data/syncroot/env/bin.src/mixins/package-manager'
import '/data/syncroot/env/bin.src/mixins/test'

globalThis.PACKAGE_MANAGER = 'bun'

app
  .cmd('test')
  .add('--from [value=1]', 'Source', ['1', '2'])
  .add((options) => {
    console.log({
      options,
    })
  })
